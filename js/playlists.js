(function(define) {
    'use strict';

    define([
        'angular',
    ], function(angular) {
        angular.module('dj.burst.playlists', [    
        ]);
        alert("playlists");
    });
})(define);
