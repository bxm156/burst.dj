(function(define) {
    'use strict';

    define([
        'angular',
    ], function(angular) {
        angular.module('dj.burst.theater', [    
        ]);
        alert("theater");
    });
})(define);
